# Sample configuration for html-to-image-server
#
# Copy to `.env` and adjust as needed. The server reads these via dotenvy.

HTML_TO_IMAGE_SERVER_ADDR=127.0.0.1:3000
HTML_TO_IMAGE_MAX_BODY=1
HTML_TO_IMAGE_FONTS_DIR=assets/fonts
